import random 

def coinflip(flips):
	tails = 0
	heads = 0
	for flip in range(1,flips+1):
		num = random.randint(1,2)
		if num == 1:
			tails += 1
		else:
			heads += 1
	print('Heads: {}'.format(heads))
	print('Tails: {}'.format(tails))
coinflip(10)
